<UserControl x:Class="BlockEditor.Views.Controls.MoveBLockOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="auto"
             HorizontalAlignment="Stretch"
             Margin="0">
    <StackPanel HorizontalAlignment="Stretch">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
            <Label FontSize="{StaticResource DefaultFontSize}" FontWeight="SemiBold" Content="Pattern: " VerticalAlignment="Center"/>
            <TextBox FontSize="{StaticResource DefaultFontSize}" Name="tbPattern" TextChanged="TextBox_TextChanged" VerticalAlignment="Center" HorizontalAlignment="Stretch" MinWidth="250"/>
        </StackPanel>
        <Label>The pattern "EEESWWN" goes in a clockwise path</Label>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
            <Label FontSize="{StaticResource DefaultFontSize}" FontWeight="SemiBold" Content="Delay: " VerticalAlignment="Center"/>
            <TextBox FontSize="{StaticResource DefaultFontSize}" Name="tbDelay" TextChanged="TextBox_TextChanged" VerticalAlignment="Center" HorizontalAlignment="Stretch" MinWidth="250"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="5,10,0,5">
            <CheckBox x:Name="cbLoop" FontWeight="SemiBold" Content="Loop" MinWidth="100" Checked="CheckBox_Checked" Unchecked="CheckBox_Checked" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="5,10,0,5">
            <CheckBox x:Name="cbRigid" FontWeight="SemiBold" Content="Rigid" MinWidth="100" Checked="CheckBox_Checked" Unchecked="CheckBox_Checked" />
        </StackPanel>

    </StackPanel>

</UserControl>
